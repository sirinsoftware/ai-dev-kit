# Project standards — @@PROJECT_NAME@@

> Fill in every `{{PLACEHOLDER}}`. Agents are told (via `AGENTS.md`) to read and
> obey this file before writing code or opening PRs. Keep it concrete — commands,
> not vibes. Generated by ai-dev-kit on @@ADK_DATE@@.

## 1. Code standards
<!-- Style guide, naming, file organization, error handling, language version,
     formatting rules, what "good" looks like, what to avoid. -->
{{CODE_STANDARDS}}

**Formatter / style command(s):**
```bash
{{FORMAT_COMMAND}}        # e.g. prettier --write . / black . / gofmt -w .
```

## 2. Commit message standards
<!-- e.g. Conventional Commits: type(scope): summary; body; footer. Examples. -->
{{COMMIT_STANDARDS}}

**Example:**
```
{{COMMIT_EXAMPLE}}
```

## 3. Pull request standards
<!-- Title format, required sections, size limits, required reviewers/checks,
     when to split a PR, linked-issue rules. -->
{{PR_STANDARDS}}

**PR description format** (reused by `20-pr-review.md`):
```
{{PR_DESCRIPTION_FORMAT}}
```

## 4. Static analysis tools
<!-- Linters, type checkers, security scanners. List the exact commands an agent
     must run and pass before considering work done. -->
| Purpose | Command |
|---|---|
| Lint        | `{{LINT_COMMAND}}` |
| Type check  | `{{TYPECHECK_COMMAND}}` |
| Security    | `{{SECURITY_SCAN_COMMAND}}` |
| Other       | `{{OTHER_ANALYSIS_COMMAND}}` |

Run all of the above and resolve findings before opening a PR.

## 5. Tests
<!-- How to run unit/integration tests, coverage expectations, where tests live. -->
**Run the test suite:**
```bash
{{TEST_COMMAND}}          # e.g. npm test / pytest / go test ./...
```
**Coverage target:** {{COVERAGE_TARGET}}

## 6. Testing on a real device / environment
<!-- Hardware, emulator, staging env, or device farm. The exact script/command an
     agent should run to validate on the real target, plus prerequisites. -->
{{REAL_DEVICE_NOTES}}

**Run on real device/environment:**
```bash
{{REAL_DEVICE_TEST_SCRIPT}}   # e.g. ./scripts/test-on-device.sh / make e2e-staging
```

**Prerequisites:** {{REAL_DEVICE_PREREQS}}
